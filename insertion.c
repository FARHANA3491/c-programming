#include <stdio.h>
void main()
{
  struct node{
 node;
    else
        newnode->next=head;
        head=newnode;        int data;
      struct node *next;
  }
}

void insertbeg(int num)
{
    struct node *newnode;
    newnode = (struct node*) malloc (size of (struct node));
    newnode->data=num;
    newnode->next=null;
    if(head==null)
        head= newnode;
}
 void insertend(int num)